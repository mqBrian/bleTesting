import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'org.fedarch.faims3',
  appName: 'faims3-qrcode-example',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
